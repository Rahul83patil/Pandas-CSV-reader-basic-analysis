# Pandas-CSV-reader-basic-analysis
Read CSV/Excel into DataFrame and inspect head/tail/types. Compute summary stats (mean, median, min, max, count). Filter rows, select columns and slice subsets. Save filtered results to CSV/excel.
